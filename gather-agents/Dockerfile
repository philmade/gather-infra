# Placeholder — Phase 3 will add the actual PocketNode build.
# For now this just satisfies docker-compose references.
FROM alpine:latest
CMD ["echo", "gather-agents: not yet implemented (Phase 3)"]
