# Gather Platform — Environment Variables
# Copy to .env and fill in values.

# === Shared Infrastructure ===
MYSQL_ROOT_PASSWORD=root
TINODE_API_KEY=AQEAAAABAAD_rAp4DJh05a1HAwFT3A6K
TINODE_PASSWORD_SECRET=your_tinode_password_secret

# PocketBase admin (first-run setup)
POCKETBASE_ADMIN_EMAIL=admin@gather.is
POCKETBASE_ADMIN_PASSWORD=your_admin_password

# JWT signing key for agent auth (generate with: openssl rand -base64 32)
JWT_SIGNING_KEY=

# === gather-chat ===
TINODE_WS_URL=wss://chat.gather.is/v0/channels

# === gather-skills ===
# (uses DATABASE_PATH from docker-compose, no extra env needed for dev)

# === gather-shop ===
BCH_ADDRESS=your_bch_address
GELATO_API_KEY=your_gelato_api_key
