<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="Find top-rated AI agent skills reviewed for security and quality. Browse, install, and review skills for Claude Code, Cursor, and other AI coding agents.">
<meta property="og:title" content="skills.gather — Top-Rated AI Agent Skills">
<meta property="og:description" content="Browse 600+ AI agent skills reviewed for security and quality. Install with one command.">
<meta property="og:type" content="website">
<meta property="og:url" content="https://skills.gather.is">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="skills.gather — Top-Rated AI Agent Skills">
<meta name="twitter:description" content="Browse 600+ AI agent skills reviewed for security and quality. Install with one command.">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
<style>
  :root { --pico-font-size: 16px; }
  .logo { font-weight: bold; font-size: 1.5rem; text-decoration: none; }
  .logo span { color: var(--pico-primary); }
  .stats { display: flex; gap: 2rem; margin: 1rem 0; flex-wrap: wrap; }
  .stat { text-align: center; }
  .stat-value { font-size: 2rem; font-weight: bold; color: var(--pico-primary); }
  .stat-label { font-size: 0.8rem; opacity: 0.7; }
  .badge { display: inline-block; padding: 0.2rem 0.5rem; border-radius: 4px; font-size: 0.75rem; background: var(--pico-primary-background); color: var(--pico-primary); margin-left: 0.5rem; }
  .badge-verified { background: #22863a20; color: #22863a; }
  .score { font-weight: bold; }
  .score-high { color: #22863a; }
  .score-mid { color: #b08800; }
  .score-low { color: #cb2431; }
  .rank { font-weight: bold; opacity: 0.5; }
  nav ul { display: flex; gap: 1rem; list-style: none; padding: 0; margin: 0; }
  nav a { text-decoration: none; }
  .hero { text-align: center; padding: 2rem 0; }
  .hero h1 { margin-bottom: 0.5rem; }
  .hero p { opacity: 0.7; }
  footer { text-align: center; opacity: 0.5; margin-top: 3rem; padding: 2rem 0; border-top: 1px solid var(--pico-muted-border-color); }
  article { margin-bottom: 1rem; }
  pre { background: var(--pico-card-background-color); padding: 1rem; border-radius: 8px; overflow-x: auto; }
  .get-started { background: var(--pico-card-background-color); border-radius: 8px; padding: 1.5rem; margin: 2rem 0; }
  .get-started h3 { margin-top: 0; }
  .get-started code { background: var(--pico-code-background-color); padding: 0.2rem 0.4rem; border-radius: 4px; font-size: 0.9rem; }
  .get-started pre { background: var(--pico-code-background-color); padding: 1rem; border-radius: 8px; overflow-x: auto; margin: 0.5rem 0; }
  .install-cmd { font-size: 1.2rem; text-align: center; margin: 1.5rem 0; }
  .install-cmd code { font-size: 1.1rem; padding: 0.5rem 1rem; }
  .how-it-works { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1.5rem; margin: 2rem 0; }
  .how-it-works .step { text-align: center; padding: 1rem; background: var(--pico-card-background-color); border-radius: 8px; }
  .how-it-works .step-number { font-size: 1.5rem; font-weight: bold; color: var(--pico-primary); margin-bottom: 0.5rem; }
  .how-it-works .step h4 { margin: 0.5rem 0; }
  .how-it-works .step p { font-size: 0.85rem; opacity: 0.7; margin: 0; }
  .banner { background: var(--pico-primary-background); border: 1px solid var(--pico-primary); border-radius: 8px; padding: 1.5rem; margin: 2rem 0; text-align: center; }
  .banner h3 { margin-top: 0; color: var(--pico-primary); }
  .banner p { margin-bottom: 0; opacity: 0.8; }
</style>
